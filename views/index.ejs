<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/css/style.css">
        <title>MY TODO APP</title>
    </head>
    <body class="font-mono bg-gray-200 tracking-widest relative">
        <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 translate-y-1/2">
            <h1 class="text-3xl text-blue-800 p-5 font-bold">TODOAPP</h1>
            <form action="/" method="POST" class="p-5">
                TODO <input type="text" name="name" class="border-black border-2 rounded-md focus:outline-none mt-3 mb-3">
                <input type="submit" value="ADD" class="btn bg-white border-black border-2 shadow-md hover:shadow-none hover:bg-black hover:text-white transition ease-out duration-500 sm:mt-2">
            </form>
            <h2 class="pl-4 pt-4">TASKLIST</h2>
            <ol>
                <% todos.forEach((value) => { %>
                    <li class="pl-10 pt-5">
                        <span class="todo"><%= value.name %></span>
                        <button class="ml-3">
                            <a href="/delete/<%= value.id %>" class="btn text-red-500 border-red-500 border-2 bg-white shadow-md hover:bg-red-500 hover:text-white transition ease-out duration-500">DELETE</a>
                        </button>
                    </li>
                <% }); %>
            </ol>
        </div>

        <script src="/js/index.js"></script>
</body>
</html>